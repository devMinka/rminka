% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mnk_obs_update.R
\name{mnk_obs_update}
\alias{mnk_obs_update}
\title{Download Minka Observations by Last Update Date}
\usage{
mnk_obs_update(day_update, ..., quiet = FALSE, limit_download = TRUE)
}
\arguments{
\item{day_update}{Date from which to retrieve updated observations.
Character string in \code{"yyyy-mm-dd"} format or a \code{Date} object.
Observations with \code{updated_at >= day_update} will be returned.}

\item{...}{
  Arguments passed on to \code{\link[=mnk_obs]{mnk_obs}}
  \describe{
    \item{\code{query}}{a generic query string for the 'q' parameter.}
    \item{\code{taxon_name}}{a character string with the taxon name (common or
scientific).}
    \item{\code{taxon_id}}{a numeric ID for the taxon.}
    \item{\code{user_id}}{a numeric ID for a specific user.}
    \item{\code{project_id}}{a numeric ID for a specific project.}
    \item{\code{place_id}}{a numeric ID for a specific place.}
    \item{\code{endemic}}{a logical value. Filters for endemic species.}
    \item{\code{introduced}}{a logical value. Filters for introduced species.}
    \item{\code{threatened}}{a logical value. Filters for threatened species.}
    \item{\code{quality}}{a character string. Must be 'casual' or 'research'.}
    \item{\code{geo}}{a logical value. If TRUE, filters for observations with
coordinates.}
    \item{\code{annotation}}{a numeric vector of length 2 (term_id, term_value_id).}
    \item{\code{bounds}}{a bounding box. Accepts an sf object with CRS EPSG:4326
(WGS84) or a numeric vector c(nelat, nelng, swlat, swlng).}
  }}

\item{quiet}{Logical. If \code{TRUE}, suppresses console progress messages.
Default \code{FALSE}.}

\item{limit_download}{Logical. If \code{TRUE} (default), each subdivided
request is capped at 10,000 records as per API limitation.}
}
\value{
A \code{tibble} with one row per observation and the same columns
  documented in \code{\link{mnk_obs}}. Returns an empty tibble if no data
  is found.
}
\description{
Downloads observation data from the Minka API filtering by
  last update date. It automatically subdivides requests by month and day
  to avoid the 10,000 record limit per request imposed by the Minka API.
}
\details{
This is a wrapper around \code{\link{mnk_obs}} that uses the
  \code{updated_at} filter. If the total number of records since
  \code{day_update} exceeds 10,000, the function splits the query by
  year/month, and if necessary, by day.
}
\examples{
\dontrun{
# Download all observations updated since a date
obs <- mnk_obs_update("2026-03-31", taxon_name = "Diplodus vulgaris")

# Use with bounds (must be EPSG:4326)
barcelona <- c(41.5, 2.3, 41.2, 2.0)
obs_bc <- mnk_obs_update("2024-01-01",
  bounds = barcelona, quiet = TRUE)
}
}
\seealso{
\code{\link{mnk_obs}}, \code{\link{mnk_obs_byday}}
}
\concept{minka download functions}
